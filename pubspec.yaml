name: project
description: Um aplicativo para gerenciar notas e horas complementares.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0  # Versão compatível do Provider

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/

